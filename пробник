a, b, c, d = int(input()), int(input()), int(input()), int(input());
for j in range(a, b + 2):
    for i in range(c, d + 1):
        if j == a:
            if (i == c) and (j == a):
                if i == d:
                    print('', end='\t')
                    print(i)
                else:
                    print('', end='\t')
                    print(i, end='\t')
            elif i == d:
                print(i)
                print(j, end='\t')
            else:
                print(i, end='\t')
        else:
            if j == a + 1:
                if (a == b) and (c == d):
                    print(j-1, end='\t')
                    
                    
# Две половинки!
# На вход программе подается строка текста.
# Напишите программу, которая разрежет ее на две равные части, переставит их местами и выведет на экран.
# Если длина строки нечетная, то длина первой части должна быть на один символ больше.

# Решение через if и for.
st = input()    # ввод строки
n1 = ''         # присвоение переменным значение пустой строки
n2 = ''         #
if len(st) % 2 == 0:     # если в строке четное количество символов,то
    for i in range (len(st) // 2):    # через цикл разделяем половинки на символы
        n1 += st[i]    # складываем в переменые
    for j in range (len(st) // 2, len(st)):
        n2 += st[j]
elif len(st) % 2 != 0:    # если в строке нечетное количество символов
    for k in range (len(st) // 2 + 1):
        n1 += st[k]
    for l in range (len(st) // 2 + 1, len(st)):
        n2 += st[l]
print(n2, n1, sep = '')    # вывод на экран

# решение через срезы 
s = input()
s1 = s[len(s) // 2 + len(s) % 2 : ]
s2 = s[ : len(s) // 2 + len(s) % 2]
print(s1 + s2)


# Заглавные буквы.
# На вход программе подается строка из имени и фамилии человека, разделенных одним пробелом. 
# Напишите программу, которая проверяет, что имя и фамилия начинаются с заглавной буквы.
# Программа должна вывести 'YES' если имя и фамилия начинаются с заглавной буквы и 'NO' 
# в противном случае.

s = input()    # эта программа работает правильно если в середине нет заглавных букв
ch = s.title()    # если будет например Jim McConor, то программа работает не верно.
if ch == s:
    print('YES')
else:
    print('NO')
    
# второе решение через цикл for 
# отделяется имя от фамилии и сравниваются их первые буквы с заглавными
s = input()
for i in range (len(s)):
    if s[i] == ' ':
        break
s1, s2 = s[:i], s[i + 1:]
if s1[0] == s1.title()[0] and s2[0] == s2.title()[0]:
    print('YES')
else:
    print('NO')


























    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    





